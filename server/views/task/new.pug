extends ../layouts/application.pug

block header
  = t('views.task.new.add')

block content
  +formFor()(action=route('tasks') method='post')
    +input(task, 'name', errors)(type="text")
    +textarea(task, 'description', errors)
    +select(task, statuses, 'statusId', errors)(class='form-select')
    +select(task, users, 'executorId', errors)(class='form-select')
    +select(task, labels, 'labels', errors, true)(class='form-select')
    +submit(status)(value=t('views.statuses.new.submit'))
